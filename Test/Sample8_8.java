package Test;

import java.util.Scanner;

class Sample8_8 {
    public static void main(String[] args){
        Scanner stdIn = new Scanner(System.in);

        /* 整数値を3つ入力させリストに格納後、最大値と最小値を表示する */
        int max, min;
        /* 整数値リスト */
    	int[] array = (__x__);
        
        System.out.println("整数値を3つ入力してください。");
        /* 入力された整数値をリストに格納 */
        (__x__)( (__x__) ){
        	// 値を出力
            System.out.print((i+1) + "つ目の整数値：");
        	//リストに格納
        	(__x__);
        }

        max = min = array[0];
    	// リスト分をループし、最大値、最小値を格納
        for (int i=1; i < array.length; i++){
        	// 現在の最大値がリスト[ インデックス:i ] より大きい場合
            (__x__) ( (__x__) ){
               (__x__);
            }
        	// 現在の最小値がリスト[ インデックス:i ] より小さい場合
            (__x__) ( (__x__) ){
               (__x__);
            }
        }
    
        /* 最大値を表示 */
        System.out.println("最大値：" + (__x__));
        
        /* 最小値を表示 */
        System.out.println("最小値：" + (__x__));
    }
}
